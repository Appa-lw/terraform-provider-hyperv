image: sjdweb/go-docker-build:1.10

stages:
  - test
  - build

before_script:
  - GOPATH=. dep ensure -vendor-only

unit_tests:
  stage: test
  script:
    - GOPATH-. make test

build:
  stage: build
  script:
    - GOPATH-. make
